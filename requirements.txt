# Dépendances réelles de l'API.
#
# Ce fichier était un `pip freeze` de poste de dev : 56 paquets, dont
# FlareSolverr, DrissionPage, openpyxl ou pefile que le code n'importe nulle
# part. Seuls les imports réels de src/ + main.py + wsgi.py restent.
#
# requests : la version 2.32.0 précédemment pinnée est yanked sur PyPI
# (conflit avec la mitigation de CVE-2024-35195).

beautifulsoup4==4.14.2
cloudscraper==1.2.71
Flask==3.1.2
Flask-Cors==6.0.5
lxml==6.0.2

# Volontairement pas épinglé au patch : 3.14 exige Python >= 3.10, or le
# `python3` de la machine est en 3.9 (l'image Docker, elle, est en 3.12).
# La plage laisse pip choisir 3.13 sur 3.9 et 3.14 sur 3.12.
RapidFuzz>=3.13,<4

requests==2.32.5

# Serveur WSGI utilisé en conteneur (cf. wsgi.py). Le serveur de dev Flask
# de main.py n'en a pas besoin.
waitress==3.0.2
